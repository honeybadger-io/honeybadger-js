{
	"extends": "../../tsconfig.base.json",
	"include": ["src/**/*.ts"],
	"compilerOptions": {
		// Generate d.ts files
		"declaration": true,
		// Output declaration files to /dist
		"declarationDir": "dist",
		// Allow importing JSON
		"resolveJsonModule": true,
		"moduleResolution": "node",
	}
}