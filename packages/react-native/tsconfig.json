{
	"extends": "../../tsconfig.base.json",
	"include": ["index.ts"],
	"compilerOptions": {
		// Generate d.ts files
		"declaration": true,
		// Output files to /dist
		"outDir": "dist",
		// Allow importing JSON
		"resolveJsonModule": true,
	}
}