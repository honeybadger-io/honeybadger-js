{
	"include": ["index.js"],
	"compilerOptions": {
		// Read .js files
		"allowJs": true,
		// Generate d.ts files
		"declaration": true,
		// Only generate d.ts files
		"emitDeclarationOnly": true,
		// Don't complain about conflicting types in our dependencies
		"skipLibCheck": true,
	}
}